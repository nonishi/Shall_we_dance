<% if post.favorited_by?(current_user) %>
  <%= link_to "気になる解除", post_favorites_path(post), method: :delete, remote: true, class: "btn btn-danger" %>
<% else %>
  <%= link_to "気になる", post_favorites_path(post), method: :post, remote: true, class: "btn btn-info" %>
<% end %>