<% if post.favorited_by?(current_user) %>
  <%= link_to "いいね解除", post_favorites_path(post), method: :delete, remote: true, class: "btn btn-secondary" %>
<% else %>
  <%= link_to "いいね", post_favorites_path(post), method: :post, remote: true, class: "btn btn-danger" %>
<% end %>