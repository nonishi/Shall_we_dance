<div class="container">
  <div class="row">
    <div class="col-6 mx-auto my-5">
      <h2 class="text-center mb-3">入力情報確認</h2>
      <table class="table mt-5">
        <tr>
          <th>名前</th>
          <td><%= params[:user][:name] %></td>
        </tr>
        <tr>
          <th>メールアドレス</th>
          <td><%= params[:user][:email] %></td>
        </tr>
        <tr>
          <th>募集対象</th>
          <td>
            <% if params[:user][:target] == "true" %>
              リーダー
            <% else %>
              パートナー
            <% end %>
          </td>
        </tr>
        <tr>
          <th>プロ/アマ</th>
          <td><%= params[:user][:status] %></td>
        </tr>
        <tr>
          <th>年齢</th>
          <td><%= params[:user][:age] %>歳</td>
        </tr>
        <tr>
          <th>身長</th>
          <td><%= params[:user][:height] %>cm</td>
        </tr>
        <tr>
          <th>地域</th>
          <td><%= params[:user][:area] %></td>
        </tr>
        <tr>
          <th>社交ダンス歴</th>
          <td><%= params[:user][:experience] %></td>
        </tr>
        <tr>
          <th>学連経験</th>
          <td><%= params[:user][:club_status] %></td>
        </tr>
        <tr>
          <th>その他(持ち級など)</th>
          <td><%= params[:user][:introduction] %></td>
        </tr>
        <tr>
          <th>パスワード</th>
          <td><%= @password %></td>
        </tr>
      </table>
      <%= form_with model: @user, url: user_registration_path, id: 'new_user', class: 'new_user', local: true do |f| %>
        <%= f.hidden_field :name, :value => params[:user][:name] %>
        <%= f.hidden_field :email, :value => params[:user][:email] %>
        <%= f.hidden_field :target, :value => params[:user][:target] %>
        <%= f.hidden_field :status, :value => params[:user][:status] %>
        <%= f.hidden_field :age, :value => params[:user][:age] %>
        <%= f.hidden_field :height, :value => params[:user][:height] %>
        <%= f.hidden_field :area, :value => params[:user][:area] %>
        <%= f.hidden_field :experience, :value => params[:user][:experience] %>
        <%= f.hidden_field :club_status, :value => params[:user][:club_status] %>
        <%= f.hidden_field :introduction, :value => params[:user][:introduction] %>
        <%= f.hidden_field :password, :value => params[:user][:password] %>
        <div class="actions text-center">
          <%= f.submit "修正する", class: "btn btn-danger", name: :back %>
          <%= f.submit "新規登録", class: "btn btn-success" %>
        </div>
      <% end %>
    </div>
  </div>
</div>